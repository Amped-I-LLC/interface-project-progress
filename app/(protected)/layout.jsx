/* ============================================================
   Protected Layout — Full Width (no sidebar/topbar)
   Auth is enforced by proxy.js before this renders.
   This interface uses a custom full-width header layout
   matching the original HTML design.
   ============================================================ */
export default function ProtectedLayout({ children }) {
  return <>{children}</>
}
